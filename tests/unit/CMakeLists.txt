add_executable(capture_unit_tests CaptureTests.cpp)
target_link_libraries(capture_unit_tests PRIVATE sdrcal_capture)
target_compile_features(capture_unit_tests PRIVATE cxx_std_20)

if(MSVC)
    target_compile_options(capture_unit_tests PRIVATE /W4 /permissive-)
else()
    target_compile_options(capture_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
endif()

add_test(NAME capture_unit_tests COMMAND capture_unit_tests)

if(TARGET sdrcal_core)
    add_executable(carrier_estimator_unit_tests CarrierEstimatorTests.cpp)
    target_link_libraries(carrier_estimator_unit_tests PRIVATE sdrcal_core)
    target_compile_features(carrier_estimator_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(carrier_estimator_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            carrier_estimator_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME carrier_estimator_unit_tests COMMAND carrier_estimator_unit_tests)

    add_executable(observation_acceptance_unit_tests ObservationAcceptanceTests.cpp)
    target_link_libraries(observation_acceptance_unit_tests PRIVATE sdrcal_core)
    target_compile_features(observation_acceptance_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(observation_acceptance_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            observation_acceptance_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME observation_acceptance_unit_tests COMMAND observation_acceptance_unit_tests)

    add_executable(model_uncertainty_unit_tests ModelUncertaintyTests.cpp)
    target_link_libraries(model_uncertainty_unit_tests PRIVATE sdrcal_core)
    target_compile_features(model_uncertainty_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(model_uncertainty_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            model_uncertainty_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME model_uncertainty_unit_tests COMMAND model_uncertainty_unit_tests)
endif()

if(TARGET sdrcal_profile)
    add_executable(profile_engine_unit_tests ProfileEngineTests.cpp)
    target_link_libraries(profile_engine_unit_tests PRIVATE sdrcal_profile)
    target_compile_features(profile_engine_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(profile_engine_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            profile_engine_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME profile_engine_unit_tests COMMAND profile_engine_unit_tests)
endif()

if(TARGET sdrcal_reference AND TARGET sdrcal_evidence)
    add_executable(reference_evidence_unit_tests ReferenceEvidenceTests.cpp)
    target_link_libraries(reference_evidence_unit_tests PRIVATE sdrcal_reference sdrcal_evidence)
    target_compile_features(reference_evidence_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(reference_evidence_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(reference_evidence_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME reference_evidence_unit_tests COMMAND reference_evidence_unit_tests)
endif()

if(TARGET sdrcal_wsjtx)
    add_executable(wsjtx_adapter_unit_tests WsjtXAdapterTests.cpp)
    target_link_libraries(wsjtx_adapter_unit_tests PRIVATE sdrcal_wsjtx)
    target_compile_features(wsjtx_adapter_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(wsjtx_adapter_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            wsjtx_adapter_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME wsjtx_adapter_unit_tests COMMAND wsjtx_adapter_unit_tests)
endif()

if(TARGET sdrcal_application)
    add_executable(application_workflow_unit_tests ApplicationWorkflowTests.cpp)
    target_link_libraries(application_workflow_unit_tests PRIVATE sdrcal_application)
    target_compile_features(application_workflow_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(application_workflow_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            application_workflow_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME application_workflow_unit_tests COMMAND application_workflow_unit_tests)
endif()

if(TARGET sdrcal_capture_cli_args)
    add_executable(capture_cli_argument_tests CaptureCliArgumentTests.cpp)
    target_link_libraries(capture_cli_argument_tests PRIVATE sdrcal_capture_cli_args)
    target_compile_features(capture_cli_argument_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(capture_cli_argument_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            capture_cli_argument_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME capture_cli_argument_tests COMMAND capture_cli_argument_tests)
endif()

if(TARGET sdrcal_product_cli)
    add_executable(production_cli_tests ProductionCliTests.cpp)
    target_link_libraries(production_cli_tests PRIVATE sdrcal_product_cli)
    target_compile_features(production_cli_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(production_cli_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            production_cli_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME production_cli_tests COMMAND production_cli_tests)
endif()

if(TARGET sdrcal_soapy)
    add_executable(soapy_adapter_unit_tests SoapyAdapterTests.cpp)
    target_link_libraries(soapy_adapter_unit_tests PRIVATE sdrcal_soapy)
    target_compile_features(soapy_adapter_unit_tests PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(soapy_adapter_unit_tests PRIVATE /W4 /permissive-)
    else()
        target_compile_options(
            soapy_adapter_unit_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
    endif()
    add_test(NAME soapy_adapter_unit_tests COMMAND soapy_adapter_unit_tests)

    if(TARGET sdrcal_capture_cli_workflow)
        add_executable(capture_cli_workflow_tests CaptureCliWorkflowTests.cpp)
        target_link_libraries(capture_cli_workflow_tests PRIVATE sdrcal_capture_cli_workflow)
        target_compile_features(capture_cli_workflow_tests PRIVATE cxx_std_20)
        if(MSVC)
            target_compile_options(capture_cli_workflow_tests PRIVATE /W4 /permissive-)
        else()
            target_compile_options(
                capture_cli_workflow_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
        endif()
        add_test(NAME capture_cli_workflow_tests COMMAND capture_cli_workflow_tests)
    endif()
endif()
