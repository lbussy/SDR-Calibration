add_library(sdrcal_reference ReferenceRegistry.cpp)
target_include_directories(sdrcal_reference PUBLIC "${PROJECT_SOURCE_DIR}/src")
target_link_libraries(sdrcal_reference PUBLIC sdrcal_profile)
target_compile_features(sdrcal_reference PUBLIC cxx_std_20)
if(MSVC)
    target_compile_options(sdrcal_reference PRIVATE /W4 /permissive-)
else()
    target_compile_options(sdrcal_reference PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
endif()
