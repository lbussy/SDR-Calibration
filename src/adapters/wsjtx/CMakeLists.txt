add_library(sdrcal_wsjtx WsjtXAdapter.cpp)
target_include_directories(sdrcal_wsjtx PUBLIC "${PROJECT_SOURCE_DIR}/src")
target_link_libraries(sdrcal_wsjtx PUBLIC sdrcal_profile)
target_compile_features(sdrcal_wsjtx PUBLIC cxx_std_20)
if(MSVC)
    target_compile_options(sdrcal_wsjtx PRIVATE /W4 /permissive-)
else()
    target_compile_options(sdrcal_wsjtx PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
endif()
