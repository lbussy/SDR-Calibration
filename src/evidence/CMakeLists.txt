add_library(sdrcal_evidence EvidenceBundle.cpp)
target_include_directories(sdrcal_evidence PUBLIC "${PROJECT_SOURCE_DIR}/src")
target_link_libraries(sdrcal_evidence PUBLIC sdrcal_profile)
target_compile_features(sdrcal_evidence PUBLIC cxx_std_20)
if(MSVC)
    target_compile_options(sdrcal_evidence PRIVATE /W4 /permissive-)
else()
    target_compile_options(sdrcal_evidence PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
endif()
